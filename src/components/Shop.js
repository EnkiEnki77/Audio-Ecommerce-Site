import React from 'react';
import { ShopContainer, CategoryImg, CategoryTitle, ShopBtn } from './styles/Shop.styles';

const Shop = (props) => {
    return (
        <ShopContainer>
            <CategoryImg/>
            <CategoryTitle/>
            <ShopBtn/>
        </ShopContainer>
    )
};

export default Shop;
